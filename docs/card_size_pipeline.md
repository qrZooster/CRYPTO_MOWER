 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/docs/card_size_pipeline.md b/docs/card_size_pipeline.md
new file mode 100644
index 0000000000000000000000000000000000000000..8265f235cf3a90b63ea602561dafa2d83ab80dcb
--- /dev/null
+++ b/docs/card_size_pipeline.md
@@ -0,0 +1,53 @@
+# 📐 Card size pipeline (Tradition Core 2025)
+
+> Обновлённые механики размеров карточек: от TSizeMixin до CSS-токенов.
+
+## 1. Что теперь происходит при смене `size`
+
+1. `TSizeMixin.size` хранит токен (`xs`..`xl`) и вызывает `on_size_changed(old, new)` у наследника, если значение меняется.  
+   См. `bb_ctrl_sizes.py:TSizeMixin.size`.  
+2. `TCard.on_size_changed` отвечает за визуальный апдейт:  
+   - пересчитывает `card-{size}` класс корня и токены `card-header-{size}` / `card-body-{size}` / `card-footer-{size}`;  
+   - ретокенизирует автошапку (заголовок/подзаголовок) и обновляет `TIcon.size` на `CardSizeCfg.icon_px`.  
+   См. `bb_ctrl_base.py:TCard._apply_size_classes()` и `_apply_header_size_tokens()`.  
+3. CSS в `assets/bb_tc.css` содержит ровно те значения px, что прописаны в `CardSizeCfg` (паддинги, отступы, типографика). Один конфиг → единые px по всему UI.
+
+## 2. Автошапка карточки
+
+`TCardPanel._auto_header_compose()` перегенерирован так, чтобы подхватывать параметры из `CardSizeCfg`:
+- иконка (`TIcon`) получает `card._size_cfg.icon_px`;
+- `TLabel` для title/subtitle получают классы `card-title card-title-{size}` и `card-subtitle card-subtitle-{size}` вместо инлайновых `font-size`.  
+Если разработчик заранее заполняет `left_td`, автошапка отключается и карточка остаётся под вашим контролем.
+
+## 3. Кастомные шапки
+
+Для своих TLabel используйте хелперы карточки:
+
+```python
+card.apply_header_title_classes(my_title)
+card.apply_header_subtitle_classes(my_sub)
+```
+
+Они навешивают базовые классы (`card-title`, `card-subtitle`) плюс размерные токены.  
+Важно: кастомный header всё равно получает корневой `card-{size}` класс, так что внешние стили могут полагаться на размер карточки.
+
+## 4. CSS + `CardSizeCfg`
+
+| Size | Icon px | Title px | Subtitle px | Header padding (y/x) | Body padding (y/x) | Margin (y/x) |
+|------|---------|----------|-------------|----------------------|--------------------|--------------|
+| xs   | 12      | 13       | 11          | 6 / 6                | 6 / 6              | 4 / 4        |
+| sm   | 14      | 14       | 12          | 8 / 8                | 8 / 8              | 6 / 6        |
+| md   | 16      | 16       | 13          | 12 / 12              | 12 / 12            | 8 / 8        |
+| lg   | 20      | 18       | 14          | 16 / 16              | 16 / 16            | 12 / 12      |
+| xl   | 24      | 22       | 16          | 20 / 20              | 20 / 20            | 16 / 16      |
+
+Эти числа заданы в `bb_ctrl_sizes.py:CARD_SIZE_CFG` и продублированы в CSS классах `.card-*` / `.card-title-*` / `.card-subtitle-*`. Если меняете конфиг — синхронизируйте таблицу и стили.
+
+## 5. Что тестить руками
+
+1. Создайте несколько `TCard` разных размеров (xs..xl).  
+2. Проверьте, что при смене `card.size`:
+   - меняются паддинги header/body/footer;  
+   - иконка масштабируется;  
+   - токены `card-title-{size}` / `card-subtitle-{size}` применяются (см. DOM).  
+3. Для кастомного header убедитесь, что вызвали `apply_header_*` и что `card-{size}` присутствует на корневом элементе.
 
EOF
)