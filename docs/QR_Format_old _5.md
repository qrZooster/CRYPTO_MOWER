конечно, ловишь актуальную версию QR_Format со всеми нашими новыми правками (жёсткое правило по пустым строкам, корона/фасад, лог-иконки, таблица эмодзи, исключение без плашки и т.д.).
это теперь эталон 🜂

---

# 📘 QR_Format.md — Принципы форматирования Tradition Core 2025

## 🧭 1. Общая философия

**Основные принципы:**

* Код читается **быстрее, чем исполняется**.
* Визуальная структура должна **отражать архитектуру**.
* Однородность важнее личного стиля.
* Комментарии объясняют *намерение*, а не синтаксис.
* Любой файл должен быть понятен даже без IDE.

Человек должен открыть .py и сразу понимать:

* кто тут главный объект;
* какая у него ответственность;
* как он живёт во времени (жизненный цикл);
* как он взаимодействует с системой.

---

## ✍️ 2. Именование

| Элемент             | Стиль        | Пример                      | Комментарий                                |
| ------------------- | ------------ | --------------------------- | ------------------------------------------ |
| Классы              | `PascalCase` | `TComponent`, `TPage`       | Базовые типы начинаются с `T` (Delphi-дух) |
| Методы, функции     | `snake_case` | `handle_event`, `normalize` | Чистый питоновский стиль                   |
| Приватные поля      | `_lowercase` | `_active`, `_thread`        | Инкапсуляция                               |
| Константы           | `UPPER_CASE` | `TRACEBACK_ENABLED`         | Глобальные настройки модуля                |
| Свойства (совмест.) | `PascalCase` | `Owner`, `Name`             | Наследие старого движка Tradition/Delphi   |

---

## ИНСТРУКЦИЯ #2 Форматирование файлов Python (QR_Format)

### 1. Файл всегда начинается с большого баннера-шапки

Каждый .py-файл обязан начинаться с такого блока:

```python
# ======================================================================================================================
# 📁 file        : <имя_файла.py> <краткое назначение / роль файла>
# 🕒 created     : <ДД.ММ.ГГГГ ЧЧ:ММ>  (использую дату/время пользователя)
# 🎉 contains    : <что внутри: основные классы / логика>
# 🌅 project     : Tradition Core 2025 🜂
# ======================================================================================================================
```

Правила:

* Поля идут именно с этими эмодзи и в этом порядке: 📁, 🕒, 🎉, 🌅.
* В `contains` я кратко описываю содержимое файла (какие классы, зачем они).
* Первая и последняя строки баннера — длинные строки `# ===...===`. Длина баннера должна быть одинаковой по всему файлу и в проекте.
* После баннера **сразу** идёт секция импортов с комментарием `# 🚢 ...imports...`.

❗ ВАЖНО: между баннером и `# 🚢 ...imports...` **НЕ бывает пустой строки**.

✅ правильно:

```python
# ======================================================================================================================
# 📁 file        : bb_app_sys_control.py — Приложение System Control (UI + runtime)
# 🕒 created     : 26.10.2025 12:40
# 🎉 contains    : TappSysControl (ядро System Control), TlySysControl (layout), TpgMain/TpgDatabase/TpgEcho (страницы)
# 🌅 project     : Tradition Core 2025 🜂
# ======================================================================================================================
# 🚢 ...imports...
import asyncio
...
```

❌ неправильно:

```python
# ======================================================================================================================
# ...
# ======================================================================================================================

# 🚢 ...imports...
# ^ лишняя пустая строка — запрещено
```

---

### 2. Секция импортов `# 🚢 ...imports...`

Сразу за баннером идёт:

```python
# 🚢 ...imports...
import asyncio
import os
from pathlib import Path
from typing import Optional, Dict, List, Any

from bb_sys import *
from bb_application import TApplication
from bb_controls import TPage, TPanel, TCard, TButton, TLayout, TGrid, TGrid_Tr, TGrid_Td
```

Правила:

* Секция импортов всегда помечается комментом `# 🚢 ...imports...`.

* Порядок импортов:

  1. стандартная библиотека (`os`, `re`, `datetime`, `asyncio`, `pathlib`, `typing`, ...),
  2. внутренние модули Tradition Core (`bb_sys`, `bb_application`, `bb_controls`, и т.д.).

* Между блоком стандартных импортов и импортов проекта может быть ОДНА пустая строка (для читаемости). Лишних пустых строк быть не должно.

* Каждый импорт в одну строку:

  ```python
  from bb_controls import TPage, TPanel, TCard, TButton, TLayout, TGrid, TGrid_Tr, TGrid_Td
  ```

  А вот так нельзя ❌:

  ```python
  from bb_controls import (
      TPage,
      TPanel,
      ...
  )
  ```

* `from bb_sys import *` допускается там, где оно исторически принято. Новые `*` не плодим без необходимости.

* После секции импортов может идти блок глобальных констант / ENV / `__all__`.

---

### 3. Глобальные настройки, ENV, константы, `__all__`

Все важные глобальные переменные (конфиги, ENV-мапы, пути, регэкспы, флаги, константы системы) должны быть вынесены в блок с 💎.

Примеры:

```python
# 💎 ... CONFIG / CONSTS ...
DEFAULT_SERVICE_NAME = "bbscan.service"
TRACEBACK_ENABLED = True
__all__ = ["TappSysControl", "TlySysControl", "TpgMain", "TpgDatabase", "TpgEcho"]
```

или:

```python
# 💎 ... Переназначаемая ENV-мапа ...
_GLOBAL_AUTO_COUNTERS: Dict[str, int] = {}
_ENV: MutableMapping[str, str] = os.environ

def set_env_mapping(mapping: MutableMapping[str, str] | None) -> None:
    ...

def get_env_mapping() -> MutableMapping[str, str]:
    ...
```

или:

```python
# 💎 ... CONFIG / CONSTS ...
DB_CFG = {
    "host": _key("DB_HOST", "127.0.0.1"),
    "port": int(_key("DB_PORT", "3307")),
    "user": _key("DB_USER", "user123"),
    "password": _key("DB_PASSWORD", "pass"),
    "database": _key("DB_NAME", "main"),
    "autocommit": True,
    "charset": _key("DB_CHARSET", "utf8mb4"),
}
```

Правила:

* Заголовок секции обязательно содержит 💎 и осмысленное описание.

* Константы называются КАПСОМ (`DB_CFG`, `BYBIT_REST`, `MSK`, `TRACEBACK_ENABLED`).

* Родственные константы можно объявлять в одну строку — это красиво и разрешено:

  ```python
  BYBIT, PERP, USDT, BUY, SELL = "BYBIT", "PERP", "USDT", "BUY", "SELL"
  ```

* Словари-конфиги (`DB_CFG` и т.д.) оформляются многострочно с запятой на каждой строке.

* `__all__` указывается тут же, вверху модуля, в одну строку, например:

  ```python
  __all__ = ["TWebSocketServer", "TWebSocketClientExtended", "MockWebSocketClient", "TBinanceWebSocketClient", "TBybitWebSocketClient"]
  ```

---

### 4. Каждый крупный класс в файле обязан начинаться с баннера из тире

Перед определением КАЖДОГО класса (или логически цельного крупного блока) идёт баннер из трёх строк:

```python
# ----------------------------------------------------------------------------------------------------------------------
# 🧩 TappSysControl — Приложение System Control (web UI + DB + WS)
# ----------------------------------------------------------------------------------------------------------------------
class TappSysControl(TApplication):
    ...
```

Правила:

* Верхняя и нижняя строки — `# ` + длинная линия из `-`, одинаковой длины везде.
* Средняя строка:

  * начинается с эмодзи 🧩,
  * затем идёт точное имя класса (`TappSysControl`),
  * затем короткое человеческое описание роли класса.
* После нижней строки баннера СРАЗУ идёт `class ...:`. Между ними не ставим пустую строку.
* Также не добавляем пустую строку между концом одного класса и баннером следующего.

Итог: файл визуально делится на «главы».

---

принял 🙌
я обновлю целиком раздел 5 в стиле итогового документа, уже со всеми правками, которые ты сейчас попросил (включая уточнённые формулировки и пример кода). можно прямо копипастить в QR_Format.md вместо текущего раздела 5.

---

### 5. Внутри класса методы организуются в секции с плашками-точками

Мы не сыпем методы просто подряд.
Внутри класса есть жёсткая визуальная структура, и она обязательна.

Общий ожидаемый вид класса:

```python
class TCustomControl(TComponent):
    prefix = "ctl"  # базовый префикс для uid
    TAGS_WITH_ID = {"div","button","h1","h2","h3","table","tr","td","form","span","input"}
    # ⚡🛠️ ▸ __init__
    def __init__(self, Owner=None, Name=None):
        """
        Базовый визуальный контрол Tradition Core. Управляет собственным Canvas, списком дочерних контролов и DOM-идентификацией.
        Зоны:
          • Controls / Canvas — дерево подчинённых контролов и буфер вывода.
          • classes/styles/attrs — будущие HTML-атрибуты корневого тега.
        Важно:
        - Контрол сам регистрируется у родителя (Owner.add_control()).
        - Генерирует uid на основе prefix и self.id().
        Переопределяй в потомках.
        """
        super().__init__(Owner, Name)
        # --- Дерево UI ---
        self.Controls: dict[str, "TCustomControl"] = {}
        self.Canvas: list[str | "TCustomControl"] = []
        # --- Корневой тег этого контрола ---
        # единый источник правды для классов/стилей/атрибутов
        self.classes: list[str] = []  # add_class() пишет сюда
        self.styles: list[str] = []   # add_style() пишет сюда
        self.attrs: list[str] = []    # add_attr() пишет сюда ("data-x='1'")
        # --- debug / mark() (ленивая подсветка)
        self._mark_enabled: bool = False
        self._mark_palette: list[str] | None = None
        self._mark_root: "TCustomControl" | None = None
        # uid
        if self.app().debug_mode:
            self.uid = f"{self.prefix}-{self.short_hash(self.id())}"
        else:
            self.uid = self.short_hash(self.id())
        # автоподключение к родителю
        if Owner and hasattr(Owner, "add_control"):
            Owner.add_control(self)
        # ... 🔊 ...
        self.log("__init__", f"{self.__class__.__name__} {self.Name} created uid={self.uid}")
        # ⚡🛠️ TCustomControl ▸ End of __init__
    # ......................................................................................................................
    # 🎨 Рендеринг
    # ......................................................................................................................
    def render(self):
        ...
    # ......................................................................................................................
    # 📊 Метрики
    # ......................................................................................................................
    def get_metrics(self):
        ...
```

Дальше — правила.

---

#### 5.a Формат внутренней плашки-точки (секции методов)

Каждая логическая группа методов внутри класса должна начинаться с ТРЁХ строк подряд:

```python
# ......................................................................................................................
# 🚀 Жизненный цикл / Старт системы
# ......................................................................................................................
def do_start(self):
    ...
```

Структура плашки:

1. строка из `# ` + много `.`
2. строка вида `# <эмодзи> <название секции>` (человекочитаемое)
3. строка снова из точек

Правила:

* Внутри класса секции размечаются только точками `.`.
  Тире `-` используется ТОЛЬКО для баннеров уровня файла и баннеров перед `class ...`.
* Название секции должно объяснять роль методов в ней:

  * `🏷️ Идентичность приложения`
  * `🚀 Жизненный цикл / Старт системы`
  * `📡 Подписки и события`
  * `🎨 Рендеринг страницы`
  * `📊 Метрики`
  * `🔁 Переподключение WS`
  * `🔰 mark* debug-подсветка`
  * и т.п.

---

#### 5.b Позиция и пустые строки вокруг плашек внутри класса

Жёсткие правила:

* Между концом предыдущего метода и началом новой плашки-точки НЕТ пустой строки.
* Между плашкой-точкой и первым методом этой секции НЕТ пустой строки.

✅ правильно:

```python
def do_pages(self):
    ...
    self.echo("TappSysControl.do_pages()")
# ......................................................................................................................
# 📡 Подписки и события
# ......................................................................................................................
def setup_subscriptions(self):
    ...
    return True
```

❌ неправильно:

```python
def do_pages(self):
    ...
    self.echo("TappSysControl.do_pages()")

# ......................................................................................................................
# 📡 Подписки и события
# ......................................................................................................................

def setup_subscriptions(self):
    ...
    return True
```

А теперь новый жёсткий момент (обновление правила):

**Если между двумя функциями НЕТ новой секции-плашки, то между ними ОБЯЗАНА быть ровно одна пустая строка.**

Это касается двух случаев:

1. Два метода подряд внутри одной и той же секции класса (оба относятся к текущей плашке-точке, мы не открываем новую).
2. Две функции верхнего уровня в модуле (вне класса), и между ними нет отдельного баннера/ритуального разделителя.

Пример (обязательная одна пустая строка внутри одной секции класса):

```python
def ping(self):
    ...
    return True

def pong(self):
    ...
    return False
```

Пример (обязательная одна пустая строка между функциями модуля):

```python
def load_env():
    ...
    return env

def init_metrics():
    ...
    return True
```

Запрещено:

* без пустой строки вообще,
* больше чем одна пустая строка подряд.

---

#### 5.c Спец-правило для `__init__`

`__init__` — это священный блок. У него свой протокол.

**5.c.1 Порядок в начале класса:**

1. Сначала идут атрибуты уровня класса (`prefix = "pnl"`, множества, константы и т.д.).

2. Без пустой строки сразу идёт якорь конструктора:

   ```python
   # ⚡🛠️ ▸ __init__
   ```

3. Сразу за этим (тоже без пустой строки) идёт сам `def __init__(...)`.

❗ Между атрибутами класса и маркером `# ⚡🛠️ ▸ __init__` пустая строка запрещена.
❗ Между маркером и `def __init__(...)` пустая строка запрещена.

**5.c.2 У `__init__` НИКОГДА не бывает плашки-точки.**
Он не заворачивается в тройной блок из точек. Маркер `# ⚡🛠️ ▸ __init__` — отдельный ритуал, не «обычная секция».

**5.c.3 Сигнатура `__init__` всегда в одну строку.**
Запрещено переносить аргументы конструктора на новые строки.

✅ правильно:

```python
def __init__(self, Owner=None, Name=None):
```

❌ нельзя:

```python
def __init__(
    self,
    Owner=None,
    Name=None,
):
    ...
```

**5.c.4 Тело конструктора обязано идти блоками с явными комментариями `# --- ... ---`, в строгом порядке:**

1. `super().__init__(...)`
2. `# --- Дерево UI ---` → создаём `Controls`, `Canvas`
3. `# --- Корневой тег этого контрола ---` → `classes`, `styles`, `attrs`
4. `# --- debug / mark() (ленивая подсветка)` → `_mark_enabled`, `_mark_root`, ...
5. блок генерации uid
6. блок автоподключения к родителю (`Owner.add_control(self)`)
7. мини-плашка лога + `self.log(...)`
8. завершающий маркер конца конструктора

**5.c.5 Лог в конце конструктора ОБЯЗАТЕЛЕН и помечается мини-плашкой:**

```python
# ... 🔊 ...
self.log("__init__", f"{self.__class__.__name__} {self.Name} created uid={self.uid}")
```

Между `# ... 🔊 ...` и `self.log(...)` нет пустой строки.

**5.c.6 Сразу после лога идёт финальная строка конструктора:**

```python
# ⚡🛠️ TCustomControl ▸ End of __init__
```

Правила:

* Эта строка идёт НЕ через пустую строку — она прямо под `self.log(...)`.
* `<ClassName>` — реальное имя класса.

**5.c.7 После конструктора СРАЗУ идёт первая секция класса (плашка-точка), без воздуха.**

То есть хвост конструктора и начало следующей секции должны выглядеть так:

```python
# ... 🔊 ...
self.log("__init__", f"{self.__class__.__name__} {self.Name} created uid={self.uid}")
# ⚡🛠️ TCustomControl ▸ End of __init__
# ......................................................................................................................
# 🎨 Рендеринг
# ......................................................................................................................
def render(self):
    ...
```

жёстко запрещено вот так (лишний воздух):

```python
# ⚡🛠️ TCustomControl ▸ End of __init__

# ......................................................................................................................
# 🎨 Рендеринг
# ......................................................................................................................

def render(self):
    ...
```

никаких "пустых строк перевести дух". либо секция, либо код.

---

#### 5.d Строковые литералы, стили, html, текстовые сообщения — только в одну строку

Если мы присваиваем строку переменной (CSS inline-style, HTML-фрагмент, статуска, текст для `.text()` и т.д.) — делаем это В ОДНУ СТРОКУ.

✅ правильно:

```python
row_style   = "display:flex; width:100%; align-items:center;"
left_style  = "display:flex; align-items:center; padding-right:8px;"
mid_style   = "flex:1 1 auto; display:flex; align-items:center; padding-right:8px;"
right_style = "display:flex; align-items:center; margin-left:auto;"
panel.text("<b>Echo:</b> live WebSocket messages and system signals will appear here.")
```

❌ запрещено:

```python
row_style = (
    "display:flex; width:100%; align-items:center;"
)

row_style = ("display:flex; width:100%; align-items:center;")

panel.text(
    "<b>Echo:</b> live WebSocket messages and system signals will appear here."
)
```

Если строка зверски длинная — объявляем её одной строкой в переменную, потом используем переменную:

```python
status_msg = "System warning: websocket reconnecting (attempt 7/10, next retry in 5s, source=BybitClient)"
panel.text(status_msg)
```

Скобки вокруг одиночной строки запрещены — они мимикрируют под вызов функции и вносят ложную сложность.

---

#### 5.e Логи и их мини-плашки

Каждый вызов лога обязан быть помечен мини-плашкой прямо перед ним.

```python
# ... 🔊 ...
self.log("_amain", f"✅ connected to {self.url}")
```

Правила:

* Мини-плашка ВСЕГДА в формате `# ... <эмодзи> ...`
* Между мини-плашкой и вызовом лог-функции нет пустой строки.
* Вызов лог-функции (`log`, `fail`, `debug`, `echo`, `warn`) идёт в одну строку.
* Мини-плашка ставится только если сразу ниже идёт сам вызов.

Стандартные плашки:

| Метод лога        | Мини-плашка    | Значение                           |
| ----------------- | -------------- | ---------------------------------- |
| `self.log(...)`   | `# ... 🔊 ...` | нормальная операционная телеметрия |
| `self.fail(...)`  | `# ... 💥 ...` | фатальная ошибка / raise ожидается |
| `self.debug(...)` | `# ... 🔍 ...` | отладочный дамп для разработчика   |
| `self.echo(...)`  | `# ... 📢 ...` | широковещалка в UI/клиент          |
| `self.warn(...)`  | `# ... ⚠️ ...` | нестабильное состояние             |

Примеры:

```python
# ... 🔊 ...
self.log("do_start", "⚙️ Boot sequence starting...")

# ... 🔊 ...
self.log("run_web_server", f"🌐 Listening on http://{host}:{port}")

# ... 📢 ...
self.echo("Session.keep_alive()", "ping ok")

# ... ⚠️ ...
self.warn("_handle_reconnect_failure", f"reconnecting in {self.reconnect_delay}s")

# ... 💥 ...
self.fail("do_open", "❌ database connection failed: pool exhausted", e)

# ... 🔍 ...
self.debug("_amain", f"raw_frame={frame[:80]!r}")
```

Эмодзи внутри строк логов (`⚙️`, `📡`, `🌐`, `✅`, `🧭`, `🎨`, `📊`, `💾`, `🧪`, `⚠️`, `🔁`, `🕒`, `❌`, `💥`) стандартизированы по смыслу.

---

#### 5.f Запрещены "висячие" пустые строки перед комментариями внутри метода

Комментарий внутри метода обязан прилипать к коду.

Жёсткие правила:

1. После закрывающего `"""` (конца докстринга метода) сразу идёт код или комментарий. Пустой строки между `"""` и первой строкой тела метода не бывает.
2. Перед любым внутренним комментарием (`# Открываем корневой блок карточки`, `# ---------- BODY ----------`) не вставляется пустая строка.
3. Между таким комментарием и следующим кодом тоже не вставляется пустая строка.

❌ запрещено:

```python
def render(self):
    """
    ...
    """

    # Открываем корневой блок карточки
    self.tg("div", cls="card shadow-sm")
    ...
```

✅ правильно:

```python
def render(self):
    """
    Рисует карточку:
        <div class="card shadow-sm">
            <div class="card-header"> ... </div>
            <div class="card-body"> ... </div>
            <div class="card-footer text-muted small"> ... </div>
        </div>
    Правила:
    - header и footer рисуются всегда;
    - body рендерит детей, кроме header/footer;
    - если body пустой → вставляем self.body_text_default.
    """
    # Открываем корневой блок карточки
    self.tg("div", cls="card shadow-sm")
    # ---------- HEADER ----------
    self.header._render()
    self.Canvas.extend(self.header.Canvas)
    # ---------- BODY ----------
    self.tg("div", cls="card-body")
    body_rendered = False
    for child in self.Controls.values():
        if child is self.header or child is self.footer:
            continue
        child._render()
        self.Canvas.extend(child.Canvas)
        body_rendered = True
    if not body_rendered:
        self.Canvas.append(self.body_text_default)
    self.etg("div")  # </div class="card-body">
    # ---------- FOOTER ----------
    self.footer._render()
    self.Canvas.extend(self.footer.Canvas)
    # Закрываем корневой блок карточки
    self.etg("div")  # </div class="card shadow-sm">
```

---

#### 5.g Докстринги (плотность и структура)

Докстринг = техпаспорт метода. Он должен быть плотным и полезным.

Правила:

1. Внутри докстринга не делаем "пустых абзацев воздуха".
2. Формат докстринга:

   * первая строка — что это такое и зачем живёт;
   * далее блоки с заголовками `Зоны:`, `Важно:`, `Типы:`, `Поведение:` и т.д.;
   * внутри блока можно использовать `•` и `-`;
   * между заголовком блока и первым пунктом блока нет пустой строки.
3. После докстринга сразу идёт код или комментарий. Пустой строки между `"""` и кодом быть не должно.
4. Если метод — hook (точка расширения), докстринг обязан содержать фразу:
   **"Переопределяй в потомках."**

---

#### 5.h Структура головы класса

Чтобы начало класса читалось одинаково во всех файлах:

1. В начале класса объявляем атрибуты уровня класса (`prefix`, множества, капс-константы).

2. Без пустой строки ставим метку:

   ```python
   # ⚡🛠️ ▸ __init__
   ```

3. Без пустой строки сразу же идёт сам `def __init__(...)`.

4. Внутри `__init__` мы делаем блоки `# --- ... ---` в каноническом порядке (см. 5.c.4).

5. В конце `__init__`:

   * мини-плашка лога + сам лог,
   * строка `# ⚡🛠️ <ClassName> ▸ End of __init__` (без пустой строки между ними),
   * тут же сразу пошла первая плашка-точка секции (`# ..................................................................`),
   * без лишних пустых строк вообще.

---

#### 5.i Исключение без плашки: подряд идущие вспомогательные методы

Это правило обновлено.

Если у нас есть несколько небольших методов подряд и они логически относятся к одной и той же секции (т.е. между ними не должно быть новой плашки-точки), то:

* Между ними ОБЯЗАНА быть ровно одна пустая строка.
* Это относится:

  * к приватным/вспомогательным методам внутри одной секции класса,
  * к функциям верхнего уровня модуля (вне класса), если между ними нет отдельного баннера/разделителя.

Пример корректно:

```python
def _mark_apply_palette(self, family: str, palette: list[str], palette_name: str) -> "TCustomControl":
    self._mark_palette = palette
    self._mark_family_cached = family
    self._mark_palette_name = palette_name
    return self

def _resolve_mark_info(self) -> dict[str,str] | None:
    """
    Возвращает информацию для рамки и бейджа debug-подсветки.
    Используется рендером только если mark() был активирован.
    """
    app = self.app()
    ...
```

И аналогично на уровне модуля:

```python
def load_env():
    ...
    return env

def init_metrics():
    ...
    return True
```

Запрещено:

* слеплять два определения без пустой строки между ними,
* вставлять больше чем одну пустую строку.

Во всех остальных случаях визуальный разрыв делается не пустой строкой, а структурой:
либо плашка-точка секции (`# ..................................................................`), либо баннер класса/фасада.

### 6. Публичные методы ядра должны быть самообъясняющимися

Классы уровня ядра (TApplication, TappSysControl, TPage, TLayout, сетевые клиенты, WebSocket-серверы и т.п.) — это публичные интерфейсы Tradition Core.

Требования:

* Каждый ключевой публичный метод обязан иметь докстринг. Пишем по-русски, можно с эмодзи, но по делу.
* Докстринг должен отвечать:

  * "Когда вызывается этот метод?"
  * "Для чего он существует?"
  * "Что должен сделать наследник?"
* Если метод — точка расширения (override hook), докстринг ОБЯЗАН содержать фразу:
  **"Переопределяй в потомках."**
* Методы жизненного цикла (`start`, `do_start`, `do_pages`, `render`, `open`, `close`, `run_web_server`, и т.д.) обязаны быть оформлены чисто, читаемо, и разделены в классе плашками-точками.
* Даже если метод пока пустой, он не может быть "немой".

❌ плохо:

```python
def do_pages(self):
    pass
```

✅ правильно:

```python
def do_pages(self):
    """
    Переопределяй в потомках. Должен зарегистрировать страницы/лейауты приложения.
    Обычно создаёт TPage(...) и TLayout(...), и тем самым фиксирует навигацию.
    """
    pass
```

---

### 7. Финальная плашка файла

Каждый файл Tradition Core заканчивается заключительным баннером-закрывашкой. Она даёт человеку ощущение завершённого модуля и контроль версии.

Формат:

```python
# ======================================================================================================================
# 📁🌄 bb_ws_extended.py 🜂 The End — See You Next Session 2025 💹 Tradition Core 2025.10
# ======================================================================================================================
```

Правила:

* Тот же стиль `# ===...===` по ширине.
* В середине: имя файла, 🜂, короткое "The End" сообщение, и ревизия движка (`Tradition Core 2025.xx`).
* Это всегда последний блок файла.

---

### 8. DEADLY RULE 💀 про пустые строки (BlankLine Death Rule)

Это критично.

**8.1 Пустые строки между структурными блоками КАТЕГОРИЧЕСКИ запрещены.**
В частности запрещено:

* ПУСТАЯ СТРОКА между баннером файла и секцией импортов.
* ПУСТАЯ СТРОКА между строкой `# ⚡🛠️ ▸ __init__` и `def __init__(...)`.
* ПУСТАЯ СТРОКА между концом метода и началом плашки-точки (`# ...................................................................`).
* ПУСТАЯ СТРОКА между плашкой-точкой и первым методом в секции.
* ПУСТАЯ СТРОКА между закрывающим `"""` докстринга и первой строкой тела метода.
* ПУСТАЯ СТРОКА перед внутренним комментарием (`# ---------- BODY ----------`) внутри метода.
* ПУСТАЯ СТРОКА перед мини-плашкой лога `# ... 🔊 ...`.
* ПУСТАЯ СТРОКА между мини-плашкой и вызовом лога.
* ПУСТАЯ СТРОКА перед завершающей строкой конструктора `# ⚡🛠️ <ClassName> ▸ End of __init__`.

**8.2 ЛЮБОЙ разрыв смысла оформляется не пустой строкой, а явной плашкой.**
Если ты хочешь визуально отделить блок:
НЕ делай пустую строку → делай плашку-точку (для секции методов)
или делай локальный комментарий-блок `# --- ... ---` внутри конструктора/method-а.

**8.3 Единственное разрешённое исключение:**
Если подряд идут два мелких `def` без плашки-точки между ними (обычно приватные хелперы типа `_foo()` / `_bar()`), допускается одна пустая строка между ними. Это исключение документировано в правиле 5.i.
⚠️ Это единственное исключение.
В остальных местах пустые строки считаются нарушением QR_Format.

---

### 9. Таблица эмодзи (семантика и резервирование)

| Роль / Сущность              | Эмодзи | Архетип / Образ                 | Значение в системе                            |
| ---------------------------- | :----: | ------------------------------- | --------------------------------------------- |
| 🧩 **Класс**                 |   🧩   | Архитектор / Структура          | Опорный компонент архитектуры                 |
| 💎 **Const / CONFIG**        |   💎   | Реликвия / Константа            | Неподвижная истина                            |
| 🚢 **Imports**               |   🚢   | Корабль из других миров         | Внешние зависимости, ресурсы                  |
| ⚡🛠️ ****init** маркер**     |  ⚡🛠️  | Инженер сборки                  | Старт конструктора класса                     |
| 🏛️👑 **Application Facade** |  🏛️👑 | Дворец / Центр власти           | Главная точка входа в систему (Application()) |
| 🔮 **Config / Schema**       |   🔮   | Оракул                          | Хранитель знаний окружения и схем             |
| 🧱 **Database**              |   🧱   | Фундамент / Каменная библиотека | Персистентный слой данных                     |
| 🎨 **UI/Render**             |   🎨   | Оформитель                      | Вёрстка / рендер DOM / визуальные панели      |
| 📡 **WebSocket / Stream**    |   📡   | Антенна                         | Каналы данных в реальном времени              |
| 🚀 **Start()**               |   🚀   | Запуск / Пробуждение            | Инициализация, старт цикла жизни              |
| 🔥 **Shutdown()**            |   🔥   | Уход в сумрак                   | Завершение, остановка                         |
| 🎬 **Scenario / Run step**   |   🎬   | Сцена                           | Важная фаза сценария исполнения               |
| 💥 **Fail / Fatal**          |   💥   | Взрыв                           | Критическая ошибка                            |
| ⚠️ **Warn**                  |   ⚠️   | Жёлтый сигнал                   | Нестабильное состояние                        |
| 🔍 **Debug**                 |   🔍   | Следователь                     | Диагностика для разработчика                  |
| 📢 **Echo / Broadcast**      |   📢   | Мегафон                         | Сообщение наружу, широковещалка               |
| 🔊 **Log / Telemetry**       |   🔊   | Громкоговоритель                | Телеметрия работы компонента                  |
| 🧪 **Mock / Test**           |   🧪   | Лаборатория                     | Тестовые режимы, синтетические данные         |
| 📊 **Metrics**               |   📊   | Аналитик                        | Метрики, статистика                           |
| 💾 **Storage / Schema**      |   💾   | Архив                           | Сохранение/чтение БД, работа со схемой        |
| 🧭 **Routing / Pages**       |   🧭   | Навигатор                       | Регистрация страниц, навигация                |
| 🌐 **Listener / Server**     |   🌐   | Сервер на границе мира          | HTTP/WS слушатель, bind на порт               |
| 🔁 **Retry / Reconnect**     |   🔁   | Цикличность                     | Переподключение, повторная попытка            |
| 🕒 **Delay / Wait**          |   🕒   | Таймер                          | Планируемая задержка                          |
| ❌ **Limit / Reject**         |    ❌   | Стоп-знак                       | Отказ в операции, лимит исчерпан              |

**Зарезервированные (их семантика фиксирована и не должна меняться):**

* 🧩 — перед именем класса в баннере класса.
* 💎 — заголовки блоков глобальных констант / CONFIG / ENV.
* 🚢 — заголовок секции импортов.
* ⚡🛠️ — маркер начала конструктора `__init__`.
* 🏛️👑 — в баннере Application Facade (`def Application(): ...`).
* 🔊 / 💥 / 🔍 / 📢 / ⚠️ — мини-плашки для логов (`# ... 🔊 ...`, `# ... 💥 ...`, `# ... 🔍 ...`, `# ... 📢 ...`, `# ... ⚠️ ...`).
* 🎨 — заголовок секции рендеринга внутри класса.
* 📊 — заголовок секции метрик внутри класса.
* 🚀 — используется в докстрингах и логах для старта/инициализации.
* 🔥 — используется в закрытии / выключении (`CloseApplication` и т.п.).
* 🎬 — допустимо использовать как особые метки важных фаз исполнения.

---

## 💡 Итоговый чеклист QR_Format (коротко)

* Файл открывается баннером из `# ===...===`, потом сразу `# 🚢 ...imports...`. Никаких пустых строк между ними.
* Импорты: стандартная библиотека → пустая строка (опционально) → модули проекта.
* После импортов: блок 💎 CONFIG / CONSTS / ENV / `__all__`.
* Каждый класс объявляется под баннером `# ---`, со строкой `# 🧩 ClassName — описание`.
* Начало класса = атрибуты класса → без пустой строки `# ⚡🛠️ ▸ __init__` → без пустой строки `def __init__(...)` (сигнатура в одну строку).
* Внутри `__init__`: тело разбивается `# --- ... ---`, лог с мини-плашкой (`# ... 🔊 ...` и т.п.), потом `# ⚡🛠️ <ClassName> ▸ End of __init__`.
* После конструктора идут секции методов, каждая секция начинается плашкой-точкой (три строки с `.`) без лишних пустых строк до/после.
* Докстринги плотные, без «воздуха». После `"""` сразу идёт код или комментарий.
* Внутри методов запрещены пустые строки перед комментариями. Комментарии должны прилипать к коду.
* Строковые литералы, HTML, CSS, тексты для `.text()` и т.д. всегда в одну строку. Без круглых скобок вокруг одиночной строки.
* Логи всегда сопровождаются мини-плашкой (`# ... 🔊 ...`, `# ... 💥 ...`, `# ... 🔍 ...`, `# ... 📢 ...`, `# ... ⚠️ ...`) без пустых строк.
* В конце файла всегда стоит финальный баннер `"The End — See You Next Session"` с `# ===...===`.

---

Это — QR_Format Tradition Core 2025.10 🜂
Это промышленный визуальный стандарт движка.
Это то, как код будет читаться через год без нас — и всё равно будет понятен.
