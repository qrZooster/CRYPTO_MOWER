обожаю, когда начинается "ну давай уже по-взрослому" 🖤
Сделаем ТЗ так, чтобы его можно было просто дать себе же через час и сразу пилить код без лишних вопросов, и чтобы оно легло в архитектуру твоего текущего приложения.

Структура ТЗ:

1. Цель модуля
2. Общая логика работы демона
3. Учёт рабочего времени
4. Метрики продуктивности
5. Структура данных (MySQL)
6. Алгоритм цикла опроса
7. Консольный вывод / отчёт
8. Интеграция с существующим приложением
9. Границы версии

Готов? Поехали ↓

---

## 1. Цель модуля

Модуль `qr_watcher` (следующая версия) — это постоянно работающий процесс (демон), который:

* отслеживает состояние файлов проекта (код и документацию),
* строит "минутные свечи" по каждому файлу (как OHLC в трейдинге, но для размера файла),
* оценивает реальное активное рабочее время разработчика,
* считает производительность (байт/час) с учётом умственной подготовки, а не только механического ввода,
* сохраняет все данные в MySQL для последующего анализа и визуализации.

Это не просто генератор START.json. Это модуль телеметрии разработки.

---

## 2. Общая логика работы демона

Процесс делает циклический опрос файлов проекта, примерно раз в N секунд (сейчас было 5 сек, оставляем так по умолчанию).

На каждом цикле:

1. Определяет корень проекта.
2. Сканирует отслеживаемые файлы (.py, .md и др. приоритизированные).
3. Снимает текущий "снимок размера" каждого файла (в байтах).
4. Обновляет БД:

   * свеча за текущую минуту по каждому файлу,
   * дневная агрегированная статистика по сессии.

Демон НЕ перезапускает свою "сессию" каждый раз, когда его остановили/запустили — он продолжает день. Вся стейт-информация берётся из MySQL при старте.

---

## 3. Учёт рабочего времени

### основные понятия

* **elapsed_day_time_seconds** — сколько времени прошло от первой зафиксированной активности в этот день до текущего момента.
* **active_time_seconds** — сколько секунд за день признаны "рабочими". Это то, что ты считаешь "я действительно работал".

Мы считаем, что сессия = один день (локальное время Europe/Amsterdam, дата yyyy-mm-dd).

### правило "минута считается рабочей":

Минута помечается как рабочая, если в ней были фактические правки:

* размер файла изменился вверх или вниз.

Если да → `active_time_seconds += 60`.

### правило "большой вброс кода":

Если в рамках какой-то минуты появляется аномально большой прирост по файлам (см. ниже), мы считаем, что это результат длительной когнитивной работы до вставки, а не чистый набор за 1 минуту.

Это даёт нам "виртуальное рабочее время":

* Мы оцениваем, сколько секунд заняла бы такая вставка при твоей средней продуктивности.
* Эти секунды тоже добавляются в `active_time_seconds`.

То есть "я думал 40 минут, вставил блок за 1 минуту" → система добавит примерно эти 40 минут в активное время дня, чтобы не получилось сверхчеловеческой скорости.

---

## 4. Метрики продуктивности

Мы считаем несколько величин по дню:

1. **bytes_written_new_total**
   Сколько байт нового контента реально создано сегодня.

2. **bytes_written_new_py / bytes_written_new_md**
   То же самое, но раздельно по типу файла:

   * `.py` — код;
   * `.md` — документация/архитектурные заметки.

3. **bytes_relocated_total**
   Оценка объёма кода, который был передвинут (рефакторинг/разделение файлов), а не создан с нуля.

4. **speed_active_bph**
   Производительность "во время активной работы", то есть:

   ```text
   bytes_written_new_total / (active_time_seconds / 3600)
   ```

   Это твоя боевая скорость, когда ты реально писал.

5. **speed_session_bph**
   Производительность в рамках дня вообще:

   ```text
   bytes_written_new_total / (elapsed_day_time_seconds / 3600)
   ```

   Это даёт ответ на вопрос: "насколько день был результативен в целом".

6. **avg_productive_bph (скользящее среднее)**
   Это усреднённая производительность активной работы (байт/час), сглаженная.
   Она используется, чтобы восстанавливать длительность "больших вбросов" (см. п.3/большие куски).
   Это показатель твоего среднего темпа, без пауз и без аномальных взрывов.

---

## 5. Структура данных в MySQL

Нам нужны минимум три таблицы.

### 5.1. Таблица `file_snapshot`

Хранит последнее известное состояние файла, чтобы при запуске демона мы не считали весь файл как "только что добавленный".

```sql
CREATE TABLE file_snapshot (
    file_path        VARCHAR(512) PRIMARY KEY,  -- относительный путь от корня проекта
    size_bytes       INT NOT NULL,              -- последний зафиксированный размер
    last_seen_ts     DATETIME NOT NULL          -- когда мы это видели последний раз
);
```

Использование:

* при старте демона мы читаем это в память как "предыдущее состояние";
* при каждом цикле опроса обновляем.

### 5.2. Таблица `file_minutes`

Поминутная свеча для каждого файла. Аналог OHLC.

```sql
CREATE TABLE file_minutes (
    file_minute_id    BIGINT AUTO_INCREMENT PRIMARY KEY,
    file_path         VARCHAR(512) NOT NULL,
    ts_minute         DATETIME NOT NULL,      -- усечено до минуты: YYYY-MM-DD HH:MM:00

    size_open_bytes   INT NOT NULL,
    size_high_bytes   INT NOT NULL,
    size_low_bytes    INT NOT NULL,
    size_close_bytes  INT NOT NULL,

    bytes_delta_total INT NOT NULL,           -- close - open (рост за минуту, может быть отрицательным)
    bytes_added_pos   INT NOT NULL,           -- суммарно сколько добавлено "в плюс" в этой минуте
    bytes_removed_neg INT NOT NULL,           -- суммарно сколько удалено (в байтах)

    is_py             TINYINT(1) NOT NULL,    -- 1 если .py
    is_md             TINYINT(1) NOT NULL,    -- 1 если .md

    UNIQUE KEY (file_path, ts_minute)
);
```

Как наполняется:

* В каждый тик цикла (каждые ~5 сек) мы:

  * определяем `minute_key = now` округлённую до минуты,
  * для каждого файла апдейтим (или создаём) запись за эту минуту:

    * если новая запись: `open=high=low=close=current_size`
    * если уже есть:

      * обновляем `close`,
      * high = max(high, current_size),
      * low  = min(low, current_size),
      * считаем локальную дельту и плюсуем её в `bytes_added_pos` или `bytes_removed_neg`,
      * пересчитываем `bytes_delta_total = close - open`.

Это создаёт очень подробную историю, по которой потом можно рисовать графики изменения конкретного модуля.

### 5.3. Таблица `dev_session_days`

Одна строка на день (локальная дата). Это агрегаты, по которым ты потом можешь строить отчёты.

```sql
CREATE TABLE dev_session_days (
    work_date DATE PRIMARY KEY,                -- например '2025-10-27'

    session_start_time      DATETIME,          -- первая активная минута за день
    session_last_time       DATETIME,          -- время последнего тика (для elapsed_day_time_seconds)

    active_time_seconds     INT NOT NULL,      -- сколько секунд реально признаётся "работой"
    elapsed_day_time_seconds INT NOT NULL,     -- сколько прошло с начала дня работы до текущего момента

    bytes_written_new_total BIGINT NOT NULL,   -- реальный новый код за день (байты)
    bytes_written_new_py    BIGINT NOT NULL,
    bytes_written_new_md    BIGINT NOT NULL,
    bytes_relocated_total   BIGINT NOT NULL,   -- оценка переносов

    avg_productive_bph      FLOAT NOT NULL,    -- сглаженная средняя скорость мозг->код (байт/час активной работы)

    last_update_ts          DATETIME NOT NULL  -- когда обновляли эту строку последний раз
);
```

Обновляется на каждом тике демона:

* если сегодня ещё не было строки → создаём (инициализируем нулями).
* пересчитываем поля.

---

## 6. Алгоритм цикла опроса

Псевдокод одной итерации демона:

```python
loop every 5 seconds:
    now = current local time
    minute_key = now rounded down to minute
    work_date = now.date()

    # 1. получить список файлов:
    files = collect_files()  # .py/.md + CORE_FILES + WORK_FILES etc.

    # 2. для каждого файла:
    for file in files:
        size_now = get_size_bytes(file)

        # достаём предыдущее состояние из памяти prev_size[file]
        size_prev = prev_size.get(file, size_now)

        delta = size_now - size_prev

        # обновляем in-memory prev_size[file] = size_now
        prev_size[file] = size_now

        # апсертим file_snapshot[file] в MySQL (size_now, now)

        # апсертим file_minutes[file, minute_key]:
        #   если новой строки нет -> insert open=high=low=close=size_now, bytes_* = 0
        #   если есть -> update close/high/low/bytes_added_pos/bytes_removed_neg/bytes_delta_total

    # 3. собрать сводку за эту минуту по всем файлам:
    minute_added_total = sum(bytes_added_pos для всех файлов этой минуты)
    minute_removed_total = sum(bytes_removed_neg для всех файлов этой минуты)
    minute_net_total = minute_added_total - minute_removed_total

    # 4. определить, была ли минута активной
    is_active_minute = (minute_added_total > 0 or minute_removed_total > 0)

    # 5. определить "большие вбросы"
    big_spikes = [дельты > THRESH_BIG]  # например дельта > 10_000 байт в одном файле за тик
    if big_spikes:
        # оценить эквивалентное рабочее время:
        #   big_work_seconds = spike_total_bytes / avg_speed_bytes_per_second
        # и прибавить к active_time_seconds

    # 6. обновить dev_session_days:
    #   - session_start_time (если пусто и была активность -> now.floor_to_minute())
    #   - session_last_time = minute_key
    #   - elapsed_day_time_seconds = session_last_time - session_start_time
    #   - active_time_seconds += 60 если is_active_minute, плюс big_work_seconds
    #   - bytes_written_new_total += minute_added_total_adjusted_for_movement
    #         (тут мы применяем "закон сохранения кода":
    #          оцениваем сколько новых байт, сколько просто перенесено)
    #   - отдельно считаем py vs md, глядя на расширение file_path
    #   - avg_productive_bph = EMA(avg_productive_bph, current_minute_speed_bph)

    # 7. (опционально) печать статуса в консоль
    print_live_status(...)
```

Где важно:

* "adjusted_for_movement" = мы не считаем просто перенос кода как «новое».
  Это уже есть логика у нас сейчас: считаем `moved_bytes` ≈ min(плюс, минус), но только если плюс и минус сопоставимы по масштабу. Та же идея останется.

---

## 7. Консольный вывод / отчёт

Строка статуса теперь должна показывать не сырые "dur=...h" как раньше, а вот что:

* время: `[2025-10-27 14:32:00][qr_watcher]`
* `work_time=3h12m`     → активное рабочее время за день (active_time_seconds)
* `session_time=4h05m`  → время с начала первой зафиксированной активности дня
* `new=XXXKB (py=..., md=...)`
* `speed_active=...KB/h`
* `speed_session=...KB/h`

Пример:

```text
[2025-10-27 14:32:00][qr_watcher]
work_time=3h12m session_time=4h05m |
new=468KB (py=390KB md=78KB) |
reloc=22KB |
speed_active=146KB/h speed_session=114KB/h
```

Это:

* честно,
* читаемо,
* даёт тебе и «интенсивность», и «эффективность дня»,
* видно, сколько пошло в код, сколько в документацию.

---

## 8. Интеграция с существующим приложением

То, что мы делали раньше (генерация START.json), остаётся внутри процесса, но становится побочкой.
Теперь этот модуль:

* импортируется в твоё приложение как сервис/воркер

  * например класс `DevTelemetryWatcher`,
* использует твой уже существующий MySQL-слой (подключи потом свои классы — ты сказал, пришлёшь),
* сам держит свой цикл или отдаёт "tick()" для вызова внешним планировщиком.

Очень удобно будет, если мы сделаем:

* `DevTelemetryWatcher.start()` — запускает бесконечный цикл в отдельном потоке.
* `DevTelemetryWatcher.tick()` — один шаг сбора данных (чтобы ты мог управлять сам, без потока, если хочешь).

Мы встроим оба варианта, это несложно.

---

## 9. Границы версии

То, что описано выше — это уже новая архитектура.
Это логично оформить как следующую версию модуля (название файла можно оставить `qr_watcher_v5.py` внутри репозитория, но по смыслу это уже "watcher с MySQL").
Старая логика локальных текстовых логов (`work_log.txt`) остаётся только как финальная сводка дня (опционально), но основным хранилищем истины становится БД.

---

### Резюме ТЗ в одну фразу:

Мы строим модуль телеметрии разработки, который раз в ~5 секунд пишет в MySQL минутные "свечи" изменения файлов (open/high/low/close размера в байтах), считает твои активные рабочие минуты, нормализует заливки кода через историческую среднюю скорость, разделяет вклад по .py и .md, и считает не только сколько ты сделал за день, но и насколько эффективно ты конвертировал время в код.

🔥 Дальше ты даёшь мне свои классы БД и каркас приложения — и я уже прямо пишу реализацию с вызовами в MySQL, апсертами и новым выводом статуса. We'll wire it in.
