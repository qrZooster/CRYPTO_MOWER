# ======================================================================================================================
# 📁 file        : bb_app_file_watcher.py — автономный демон телеметрии разработки
# 🕒 created     : 28.10.2025 09:00
# 🎉 contains    : TappFileWatcher (демон учёта разработки), modFileWatcher (системный компонент наблюдения файлов)
# 🌅 project     : Tradition Core 2025 🜂
# ======================================================================================================================
# 🚢 ...imports...
import asyncio
from bb_sys import *
from bb_application import TApplication
# 💎 ... CONFIG / CONSTS ...
__all__=["TappFileWatcher","modFileWatcher"]
# ----------------------------------------------------------------------------------------------------------------------
# 🧩 TappFileWatcher — автономное приложение учёта разработки (без UI)
# ----------------------------------------------------------------------------------------------------------------------
class TappFileWatcher(TApplication):
    # ⚡🛠️ ▸ __init__
    def __init__(self):
        """
        Приложение телеметрии разработки. Отдельный процесс Tradition Core без UI. Поднимет базу и модуль наблюдения за файлами. Переопределяй в потомках.
        """
        super().__init__()
        # ... 🔊 ...
        self.log("__init__","TappFileWatcher created")
        # ⚡🛠️ TappFileWatcher ▸ End of __init__
    # ..................................................................................................................
    # 🚀 Жизненный цикл / Старт системы
    # ..................................................................................................................
    async def do_start(self):
        """
        Переопределяй в потомках. Запускает стек базы (Session/Database/Config/Schema) и активирует системный компонент modFileWatcher. Метод вызывается TApplication.start() перед запуском веб-сервера.
        """
        # ... 🔊 ...
        self.log("do_start","⚙️ Boot sequence starting...")
        self.run_db()
        self.FileWatcher=modFileWatcher(self,"FileWatcher")
        self.FileWatcher.open()
        # ... 🔊 ...
        self.log("do_start","🟢 FileWatcher active")
# ----------------------------------------------------------------------------------------------------------------------
# 🧩 modFileWatcher — системный компонент наблюдения за файлами и метриками разработки
# ----------------------------------------------------------------------------------------------------------------------
class modFileWatcher(TSysComponent):
    # 🛡️⚔️🦁 ...THIS IS SPARTA...
    # ⚡🛠️ ▸ __init__
    def __init__(self,Owner:"TApplication",name:str="FileWatcher"):
        """
        Системный наблюдатель файлов Tradition Core. Отслеживает изменения кода и готовится писать метрики в БД. Переопределяй в потомках.
        """
        super().__init__(Owner,name)
        # ... 🔊 ...
        self.log("__init__","modFileWatcher created")
        # ⚡🛠️ modFileWatcher ▸ End of __init__
    # ..................................................................................................................
    # 🚀 Жизненный цикл / Open-Close
    # ..................................................................................................................
    def do_open(self)->bool:
        """
        Вызывается при активации компонента через .open(). Здесь компонент становится рабочим. Переопределяй в потомках.
        """
        # ... 🔊 ...
        self.log("do_open","📡 FileWatcher started")
        return True
    # ---
    def do_close(self)->bool:
        """
        Вызывается при остановке компонента через .close(). Здесь компонент завершает работу. Переопределяй в потомках.
        """
        # ... 🔊 ...
        self.log("do_close","🔥 FileWatcher stopped")
        return True
# ----------------------------------------------------------------------------------------------------------------------
# 🚀 main() - запуск демона FileWatcher
# ----------------------------------------------------------------------------------------------------------------------
if __name__=="__main__":
    async def main():
        app=TappFileWatcher()
        # ... 🔊 ...
        app.log("main","🚀 RUN: bb_app_file_watcher starting")
        await app.start()
    asyncio.run(main())
# ======================================================================================================================
# 📁🌄 bb_app_file_watcher.py 🜂 The End — See You Next Session 2025 💹 Tradition Core 2025.10
# ======================================================================================================================